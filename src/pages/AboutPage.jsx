export default function AboutPage() {
  return (
    <section>
      <h2>About</h2>
      <p>
        This is another protected route. It exists to verify that authenticated
        navigation through React Router is working as expected.
      </p>
    </section>
  )
}
